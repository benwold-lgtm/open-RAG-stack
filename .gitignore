# Local env overrides — never commit
.env
*.env.local

# Graphify knowledge graph (local tooling, rebuilt on demand)
graphify-out/

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Draw.io backup files
*.bkp

# Phase 5 eval data — contains internal-doc content, keep local (scripts only are committed)
eval/dataset*.jsonl
eval/results*

# OS
.DS_Store
Thumbs.db
