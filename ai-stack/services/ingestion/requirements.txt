# Pinned to exact versions for reproducible builds. These are the versions validated
# running on the GPU node; bump deliberately and re-test ingestion after any change.
# To refresh after an intentional bump: build the image, then
#   docker run --rm <image> pip freeze | grep -iE '<pkg>'
# and copy the resolved version here. (A full lockfile is a possible future step.)
fastapi==0.111.0
uvicorn==0.30.0
httpx==0.27.0
pydantic==2.13.4
qdrant-client==1.9.1
python-dotenv==1.0.0
aiosqlite==0.20.0
tenacity==8.3.0
crawl4ai==0.4.247
pymupdf==1.28.0
pytesseract==0.3.13
python-docx==1.2.0
python-pptx==1.0.2
langchain-text-splitters==1.1.2
prometheus-fastapi-instrumentator==7.1.0  # 7.x keeps starlette<1.0, compatible with the pinned fastapi
