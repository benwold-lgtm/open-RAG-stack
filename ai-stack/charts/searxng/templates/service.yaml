apiVersion: v1
kind: Service
metadata:
  name: searxng
  namespace: searxng
spec:
  type: {{ .Values.service.type }}
  selector:
    app: searxng
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
